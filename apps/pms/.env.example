# Supabase Auth — exposé au navigateur (préfixe VITE_ requis par Vite).
# Seules la clé publique anon et l'URL sont exposées ici ; la clé
# service-role ne doit JAMAIS apparaître côté client (D13).
VITE_SUPABASE_URL=https://[project].supabase.co
VITE_SUPABASE_ANON_KEY=
